def ask_password(true_password):
    pas=input()
    a=1
    while pas!=true_pasword and a<=3:
        pas=input()
        a+=1
        if pas==true_password:
            print("UNLOCKED")
        else:
            print("BLOCKED")

true_password='qwerty'
ask_password(true_password)
